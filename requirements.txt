# Tested with Python 3.12.12. Versions pinned for reproducibility of the reported numbers.
numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.9.0
statsmodels==0.14.6
rddensity==3.0          # Cattaneo-Jansson-Ma density-discontinuity (McCrary) test, H2
matplotlib==3.10.9
nbformat==5.10.4        # notebooks/results.ipynb
python-pptx==1.0.2      # mid-presentation deck only (src/make_deck.py); not needed for the analysis/paper
# External tools (not pip): LibreOffice `soffice` to export the .pptx to PDF;
# a LaTeX distribution (pdflatex/latexmk) to build paper/main.pdf.
